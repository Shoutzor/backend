extend type Query {
    "Fetch Request"
    request(
        id: ID @eq
    ): Request
        @find

    "Fetch multiple requests"
    requests(
        where: _ @whereConditions(columns: ["played_at"])
        orderBy: _ @orderBy
    ): [Request!]!
        @paginate(defaultCount: 10)

}
